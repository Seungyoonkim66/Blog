let mongoose;_1a8‍.x([["default",()=>_1a8‍.o]]);_1a8‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);

const { Schema } = mongoose;

const PostSchema = new Schema({
    title: String,
    body: String,
    tags: [String],
    publishedDate:{
        type: Date,
        default: Date.now,
    },
});

// mongoose.miodel(스키마 이름, 스키마 객체)
const Post = mongoose.model('Post', PostSchema);
// >> 실제 DB에 만드는 컬렉션 이름 = posts (복수)
_1a8‍.d(Post);
