import React from 'react';
import styled from 'styled-components';

const HeaderBlock = `

`;

const Header = () => {
    return(
        <HeaderBlock>
        
        </HeaderBlock>
    );
}

export default Header;